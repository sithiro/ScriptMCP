---
name: db-get
description: >-
  This skill should be used when the user asks which database is active, where are functions stored,
  show database path, or wants to see the currently active ScriptMCP database.
version: 1.0.0
---

# Show the Active ScriptMCP Database

Show the path of the currently active ScriptMCP database.

Call `get_database` and present the result to the user exactly as returned.
